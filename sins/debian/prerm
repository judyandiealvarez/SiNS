#!/bin/bash
set -e

# Stop the service before removal
if systemctl is-active --quiet sins-server.service 2>/dev/null; then
    systemctl stop sins-server.service
fi

# Disable the service
if systemctl is-enabled --quiet sins-server.service 2>/dev/null; then
    systemctl disable sins-server.service
fi

